# Fraud DVC pipeline

## Setup

1. Install dependencies:

```bash
pip install dvc kaggle
```

2. Put Kaggle token in:

```bash
~/.kaggle/kaggle.json
chmod 600 ~/.kaggle/kaggle.json
```

3. Run:

```bash
dvc repro
```
